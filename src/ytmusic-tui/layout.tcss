SongsWidget, SearchWidget, PlaylistWidget, NowPlayingWidget, MetaWidget {
    border: solid white;
}

SongsWidget {
    width:1fr;
}

ContentContainer{
    width:1fr;
    height:1fr;
    background: blue;
}

Sidebar {
    width: 20;
    dock: left;
    background: red;
}

MetaWidget {
    height: 1fr;
}

PlaylistWidget {
    height: 1fr;
}
